import fs from'node:fs';import assert from'node:assert/strict';const s=fs.readFileSync('schema_v25_substitution_effective_state.sql','utf8');assert(s.includes("else e.event_at+interval '9 hours' end at_kst"));assert(s.includes("like 'substitution:%' then l.new_event_at+interval '9 hours'"));assert(s.includes("where typ='OUT' and at_kst>in_kst"));assert(!/delete\s+from\s+(public\.)?attendance_events/i.test(s));assert(!/update\s+(public\.)?attendance_events/i.test(s));console.log('substitution effective state v25: PASS');