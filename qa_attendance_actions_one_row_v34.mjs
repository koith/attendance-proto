import fs from 'node:fs';import assert from 'node:assert/strict';const s=fs.readFileSync('index.html','utf8');assert(s.includes('.att-right{display:flex; flex-direction:row;'));assert(s.includes('.att-inline-badges{display:inline-flex'));assert(s.includes('<div class="line1">${line1}${badges?'));assert(!s.includes('<div class="att-right">\n          ${badges?'));assert(s.includes('data-fix="${e.id}"'));console.log('attendance actions one row v34: PASS');
