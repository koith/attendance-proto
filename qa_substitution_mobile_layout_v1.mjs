import fs from'node:fs';import assert from'node:assert/strict';const h=fs.readFileSync('substitution.html','utf8'),i=fs.readFileSync('index.html','utf8');assert(h.includes('grid-template-columns:minmax(0,1fr) minmax(0,1fr)'));assert(h.includes('@media(max-width:430px){.row{grid-template-columns:1fr}'));assert(h.includes('input[type="datetime-local"]{width:100%;max-width:100%;min-width:0'));assert(!i.includes('>급여</a>\\n'));console.log('substitution mobile layout: PASS');