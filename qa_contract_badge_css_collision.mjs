import fs from 'node:fs';import assert from 'node:assert/strict';const s=fs.readFileSync('index.html','utf8');assert(s.includes('#success{'));assert(s.includes('#success.show{display:flex;}'));assert(!s.includes('.success{\n    position:fixed'));assert(s.includes('.badge.work, .badge.success{'));assert(s.includes('class="badge success"'));console.log('contract badge CSS collision QA PASS');
