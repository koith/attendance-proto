name: contract-overnight-v1-regression
on:
  pull_request:
    branches: [main]
  push:
    branches: [chatgpt-quick-fix-pack-5]
jobs:
  qa:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax
        run: |
          node --check employment_contract_overnight_v1.js
          node --check qa_contract_overnight_v1.mjs
      - name: Contract overnight V1 QA
        run: node qa_contract_overnight_v1.mjs
