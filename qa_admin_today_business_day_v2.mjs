import fs from 'node:fs';import assert from 'node:assert/strict';const s=fs.readFileSync('index.html','utf8');assert(s.includes('const firstTodaySession=todayComplete[0]||null'));assert(s.includes('const firstIn=firstTodaySession?.in ? {in:firstTodaySession.in} : null'));assert(!s.includes('const firstIn=todaySess.find(s=>s.in && isToday(s.in))'));console.log('overnight Today display: PASS');