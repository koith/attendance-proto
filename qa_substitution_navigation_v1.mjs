import fs from 'node:fs';import assert from 'node:assert/strict';const i=fs.readFileSync('index.html','utf8');const h=fs.readFileSync('substitution.html','utf8');assert.match(i,/id="tabSubstitution" href="substitution\.html">대타<\/a>/);assert.match(h,/href="index\.html">← 근태 화면<\/a>/);assert.match(h,/id="create"/);assert.match(h,/id="load"/);console.log('substitution navigation: PASS');