import fs from 'node:fs';import assert from 'node:assert/strict';const js=fs.readFileSync('actual_attendance.js','utf8'),css=fs.readFileSync('actual_attendance.css','utf8'),html=fs.readFileSync('actual_attendance.html','utf8');const a=js.indexOf('function renderMonth(){'),b=js.indexOf('function toAxisHour',a),m=js.slice(a,b);assert(!m.includes('day-summary'));assert(!m.includes('day-warning'));assert(m.includes('worked-dot'));assert(m.includes('issue-dot'));assert(m.includes('날짜를 누르면 상세'));assert(css.includes('.worked-dot,.issue-dot'));assert(html.includes('actual_attendance.css?v=20260921a'));assert(html.includes('actual_attendance.js?v=20260921a'));console.log('actual attendance month simple v33: PASS');
