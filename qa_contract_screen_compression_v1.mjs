import fs from 'node:fs';import assert from 'node:assert/strict';const c=fs.readFileSync('employment_contracts_v3.js','utf8'),d=fs.readFileSync('employment_contract_docs_v1.js','utf8'),h=fs.readFileSync('employment_contracts.html','utf8');assert(!c.includes('근무요일을 선택하면 시간을 입력할 수 있습니다.'));assert(!c.includes('주당 계약시간'));assert(c.includes('계약 근무시간'));assert(c.includes("'총 '+fmtMin(weeklyMinutes())"));assert(c.includes('id="nightModal"'));assert(c.includes('id="nightEnd"'));assert(c.includes('grid2 keep-two'));assert(!c.includes('야간수당을 계산해 세전 급여에 합산합니다.'));assert(!d.includes('선택된 새 계약서가 없습니다.'));assert(!d.includes('새 계약 저장 후 이 계약에 연결됩니다.'));assert(!d.includes('기존 미분류 계약서'));assert(d.includes('연결되지 않은 계약서'));assert(d.includes("classList.toggle('hidden',!(legacy||[]).length)"));assert(!h.includes('employment_contract_night_copy_v1.js'));console.log('contract screen compression PASS');