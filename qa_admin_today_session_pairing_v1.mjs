import fs from 'node:fs';import assert from 'node:assert/strict';const s=fs.readFileSync('index.html','utf8');assert(s.includes('if(LIVE && rows.length && rows[0].event_type==="OUT")'));assert(s.includes('const histEff=applyCorrections(hist.events,hist.corrections).filter(x=>x.employee_id===e.id)'));assert(s.includes('pairingRows=[...histEff,...rows]'));assert(s.includes('const todaySess=pairEvents(pairingRows)'));console.log('admin today session pairing: PASS');