import fs from 'node:fs';import assert from 'node:assert/strict';const s=fs.readFileSync('index.html','utf8');assert(s.includes('if(LIVE){'));assert(s.includes('const hist=await BE.eventsWithCorrections("2000-01-01T00:00:00'));assert(s.includes('pairingRows=applyCorrections(hist.events,hist.corrections).filter(x=>x.employee_id===e.id)'));assert(s.includes('const todaySess=pairEvents(pairingRows)'));console.log('admin today session pairing: PASS');