import fs from 'node:fs';import assert from 'node:assert/strict';const s=fs.readFileSync('index.html','utf8');assert(s.includes('if(LIVE){'));assert(s.includes('const history=await BE.eventsWithCorrections("2000-01-01T00:00:00'));assert(s.includes('historyEff=applyCorrections(history.events, history.corrections)'));assert(s.includes('historyEff.filter(x=>x.employee_id===e.id)'));assert(s.includes('const todaySess=pairEvents(pairingRows)'));assert.equal((s.match(/BE\.eventsWithCorrections\("2000-01-01T00:00:00"/g)||[]).length,1);console.log('admin today session pairing: PASS');
