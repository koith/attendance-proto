import fs from 'node:fs';import assert from 'node:assert/strict';const s=fs.readFileSync('index.html','utf8');assert(!s.includes('이 값은 <b>${ym}에만</b> 적용됩니다.'));assert(!s.includes('<label>주휴 인정주수 (예: 1주 제외→3)</label>'));assert(s.includes('<input id="maWeeks" type="hidden"'));assert(s.includes('<input id="maWage" type="hidden"'));assert(s.includes('<input id="maJh" type="hidden"'));assert(s.includes('<input id="maTax" type="hidden"'));assert(s.includes('<label>가감액 (수당+/공제−, 원)</label>'));assert(s.includes('<label>이달 사유</label>'));console.log('month adjust cleanup v31: PASS');
