import fs from 'node:fs';import assert from 'node:assert/strict';const s=fs.readFileSync('index.html','utf8');assert(s.includes('font-size:.68rem;line-height:1.15'));assert(s.includes('${fmtHM(sec)} 근무</span>'));assert.equal((s.match(/\$\{fmtHM\(sec\)\} 근무/g)||[]).length,1);assert(s.includes('근태 확인 ${issues}'));assert(!s.includes('급여 계산 보류'));console.log('payroll row final v32: PASS');
