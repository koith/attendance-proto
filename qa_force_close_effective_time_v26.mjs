import fs from'node:fs';import assert from'node:assert/strict';const s=fs.readFileSync('schema_v26_force_close_effective_time.sql','utf8');assert(s.includes("else e.event_at+interval '9 hours' end event_at_kst"));assert(s.includes("else c.new_event_at end"));assert(s.includes("like 'substitution:%'"));assert(s.includes("not exists(")&&s.includes("reason='SYSTEM_STORE_CLOSE'"));assert(!/delete\s+from\s+(public\.)?attendance_events/i.test(s));assert(!/update\s+(public\.)?attendance_events/i.test(s));console.log('force close effective time v26: PASS');