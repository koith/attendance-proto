# 대타 근무 설계 V1

## 결론
대타는 근무시간 자체를 두 직원 사이에서 직접 이동시키는 방식보다, **대타 요청 토큰**이 두 직원을 연결하고 실제 출퇴근이 그 토큰의 이행 여부를 증명하는 방식으로 처리한다.

## 상태 모델
`REQUESTED → ACCEPTED → IN_PROGRESS → COMPLETED`
예외: `REJECTED / CANCELLED / FAILED / PARTIAL`

토큰에 보관할 핵심 값:
- 요청 직원(requester_employee_id)
- 수주 직원(substitute_employee_id)
- 대타 예정 시작/종료
- 요청/승인/취소/완료 시각
- 상태
- 실제 대타 근무와 연결된 attendance session 식별자
- 부족/초과 근무시간
- 메모/사유

## UX
1. 발주 직원이 본인 근태 영역에서 `대타 요청`을 선택한다.
2. 상대 직원과 시작/종료 시간을 지정해 전송한다.
3. 수주 직원에게 대기 요청이 표시된다.
4. 수주 직원이 승인하면 토큰이 ACCEPTED가 된다.
5. 해당 시간대 실제 출퇴근을 토큰과 연결한다.
6. 종료 후 실제근무를 기준으로 COMPLETED / PARTIAL / FAILED를 판정한다.

승인만으로 근무 완료 처리하지 않는다. 최종 증거는 항상 실제 Attendance다.

## 시급제
- 발주 직원이 실제로 일하지 않은 시간은 급여에 들어가지 않는다.
- 수주 직원이 실제로 일한 시간은 수주 직원의 계약 시급으로 계산한다.
- 별도 대타 프리미엄이 필요하면 향후 계약/매장 정책으로 추가한다.
- 따라서 급여 시간을 억지로 A에서 빼서 B에 더하는 로직은 만들지 않는다. 실제근태가 이미 급여 귀속을 결정하고, 토큰은 왜 다른 직원이 근무했는지 증명한다.
- 수주 직원이 대타를 완료하지 않으면 실제 근무한 만큼만 인정하고, 전혀 근무하지 않았다면 대타 급여는 0이다. 발주 직원도 실제근무가 없으므로 해당 시간 급여는 0이며 미이행 기록이 남는다.

## 월급제
월급제는 시급처럼 예정시간을 바로 돈으로 이전하면 이중 지급/과도한 공제가 생길 수 있다. 따라서 **시간 채무/크레딧 원장**으로 먼저 처리한다.

- 자신의 근무를 대타로 넘긴 시간 → `대타 채무시간`
- 다른 직원의 대타를 수행한 시간 → `대타 크레딧시간`
- 같은 급여기간 안에서는 크레딧으로 채무시간을 상계할 수 있다.
- 월말 남은 채무시간은 `미충족 근무시간`으로 남긴다.
- 월말 초과 크레딧은 `추가근무 후보`로 남긴다.

**금전 변환은 별도 정책 확정 전 자동 계산하지 않는다.**
선배에게 확인할 항목:
1. 남은 채무시간을 월급에서 공제할지, 공제한다면 시간단가 산식은 무엇인지.
2. 초과 크레딧을 추가수당으로 지급할지, 지급한다면 시간단가/가산율은 무엇인지.
3. 지각·조퇴처럼 일부만 대타가 완료된 PARTIAL 처리 기준.

## 실패/미완료
- ACCEPTED였지만 수주 직원의 실제근무가 없음 → FAILED.
- 일부만 근무 → PARTIAL.
- 수주 직원은 실제근무하지 않은 부분의 대타비를 받지 않는다.
- 발주 직원도 직접 근무하지 않은 부분은 근무 누락으로 남는다.
- 원 요청과 승인 이력은 삭제하지 않고 감사기록으로 유지한다.

## 구현 순서
1. request/token 테이블과 승인 상태/RPC
2. 직원 본인 화면의 요청/승인 UX
3. 실제 attendance session ↔ token 연결
4. 완료/부분/실패 판정
5. 시급제 급여에 설명용 대타 출처 표시 (급여 산식은 실제근무 그대로)
6. 월급제 시간 채무/크레딧 원장
7. 월급제 금전처리는 정책 확정 후 연결
