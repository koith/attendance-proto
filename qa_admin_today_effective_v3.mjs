import fs from 'node:fs';import assert from 'node:assert/strict';const s=fs.readFileSync('index.html','utf8');assert(s.includes('eventsWithCorrections("2000-01-01T00:00:00',));assert(s.includes('const hasTodayAttendance=!!(firstIn||lastOut)'));assert(s.includes('} else if(hasTodayAttendance){'));assert(!s.includes('} else if(firstIn || lastOut || issue){'));console.log('Today effective history v3: PASS');